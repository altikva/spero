apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: spero-supervisor
  labels:
    app.kubernetes.io/name: spero
    app.kubernetes.io/part-of: spero
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: spero-supervisor
subjects:
  - kind: ServiceAccount
    name: spero
    namespace: spero-system
