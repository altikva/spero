"""The AI layer (Phase 3).

Sequenced: predictive (disk-fill, capacity, flapping) first, then LLM root-cause
and incident summaries, then the natural-language ops interface, then policy-gated
agentic remediation. Provider-pluggable, Claude as the default. Stub for now.
"""
